module roll;
import std::io;

fn String! parse_string(String rollStr){
    ByteReader br;
    br.init(rollStr);
    // return parse(&br);
    unreachable("IN PROGRESS");
}


// fn String! parse(InStream r){

// }